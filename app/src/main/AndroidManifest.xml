<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lucky.keshe">

    <uses-permission android:name="android.permission.WRITE_SETTINGS"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.RegisterActivity" />
        <activity android:name=".activity.CommonActivity" />
        <activity android:name=".activity.ManageActivity" />
        <activity android:name=".activity.MakePlanActivity" />
        <activity android:name=".activity.HistoryPlanActivity" />
        <activity android:name=".activity.MakePublishActivity" />
        <activity android:name=".activity.PresentPlanActivity" />
        <activity android:name=".activity.ReviserPasswordActivity"></activity>
    </application>

</manifest>